import { ExposeElementDirective } from './expose-element.directive';

describe('ExposeElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ExposeElementDirective();
    expect(directive).toBeTruthy();
  });
});
